version: 0.2

phases:
  install:
    commands:
      - echo Logging in to Amazon ECR...
      - cmd.exe \c "aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin 023756939836.dkr.ecr.ap-northeast-1.amazonaws.com"
     
